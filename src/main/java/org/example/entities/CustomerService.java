package org.example.entities;

public class CustomerService extends Employee{
    public CustomerService(int ID, String name, String phoneNum, String email, String userName, String password) {
        super(ID, name, phoneNum, email, userName, password);
    }
}
