package org.example;

public class costumerService extends employee {
    public costumerService(int ID, String name, String phoneNum, String email, String userName, String password) {
        super(ID, name, phoneNum, email, userName, password);
    }
}
